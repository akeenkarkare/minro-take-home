export default function Home() {
  return (
    <main style={{ padding: 40, fontFamily: "system-ui, sans-serif" }}>
      <h1>minro enrichment</h1>
      <p>UI is being built. The API is at the configured NEXT_PUBLIC_API_URL.</p>
    </main>
  );
}
